import { Button, SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";
import { Redirect } from "expo-router";

const SignOutScrn = () => {
  return (
    <SafeAreaView>
      <Text>Sign Out User?</Text>
    </SafeAreaView>
  );
};

export default SignOutScrn;

const styles = StyleSheet.create({});
